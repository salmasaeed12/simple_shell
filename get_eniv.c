/*
*#include "shell.h"
*
*char *get_env(const char *find)
*{
*int i = 0;
*len = _strlen(find);
*char **envi == environ;
*while (envi[i] == NULL)
*{
*(_strcmp(envi[i], find, len) == 0) ? (return (envi[i] + len + 1)) : i++;
*}
*return (NULL);
*}
*/
